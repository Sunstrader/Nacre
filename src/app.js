(function(){
'use strict';
const C=window.NACRE_CONTENT,Game=window.NacreGame;
const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const paths={compass:'<circle cx="12" cy="12" r="9"/><path d="m15.5 8.5-2 5-5 2 2-5Z"/>',arrow:'<path d="M4 12h16m-6-6 6 6-6 6"/>',back:'<path d="M20 12H4m6-6-6 6 6 6"/>',close:'<path d="m6 6 12 12M18 6 6 18"/>',moon:'<path d="M19.4 14.8A8 8 0 0 1 9.2 4.6 8 8 0 1 0 19.4 14.8Z"/>',sun:'<circle cx="12" cy="12" r="4"/><path d="M12 2v2m0 16v2M2 12h2m16 0h2M5 5l1.5 1.5m11 11L19 19M5 19l1.5-1.5m11-11L19 5"/>',map:'<path d="m3 5 6-2 6 3 6-2v15l-6 2-6-3-6 2Zm6-2v15m6-12v15"/>',book:'<path d="M12 5C9 2 5 3 3 4v15c3-1 6-1 9 1 3-2 6-2 9-1V4c-3-1-6-1-9 1Zm0 0v15"/>',bag:'<path d="M5 7h14l2 14H3L5 7Zm3 0V5a4 4 0 0 1 8 0v2"/>',people:'<circle cx="9" cy="7" r="3"/><path d="M2 20v-3a7 7 0 0 1 14 0v3M16 4a3 3 0 0 1 0 6m2 3c3 1 4 3 4 6"/>',image:'<rect x="3" y="3" width="18" height="18" rx="2"/><circle cx="8" cy="8" r="1.5"/><path d="m3 17 5-5 4 4 4-7 5 8"/>',save:'<path d="M4 3h13l4 4v14H3V3Zm3 0v6h10V3M7 21v-8h10v8"/>',settings:'<path d="M12 3v3m0 12v3M3 12h3m12 0h3M5.6 5.6l2 2m8.8 8.8 2 2M5.6 18.4l2-2m8.8-8.8 2-2"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/>',leaf:'<path d="M20 3C5 1 1 9 6 16s15 3 14-13ZM4 21 16 7"/>',gem:'<path d="m3 8 5-5h8l5 5-9 13L3 8Zm0 0h18M8 3l4 18 4-18"/>',gear:'<circle cx="12" cy="12" r="7"/><circle cx="12" cy="12" r="3"/><path d="M12 2v3m0 14v3M2 12h3m14 0h3"/>',bottle:'<path d="M9 3h6m-5 0v6L5 15v6h14v-6l-5-6V3M6 15h12"/>',shield:'<path d="M12 2 3 6v6c0 6 9 10 9 10s9-4 9-10V6Z"/>',sword:'<path d="m4 20 3-3m-3-3 6 6M7 17 20 4V2h-2L5 15"/>',key:'<circle cx="8" cy="8" r="5"/><path d="m12 12 9 9m-3-3 3-3m-6 0 3-3"/>',scroll:'<path d="M6 4h13v15a2 2 0 0 1-4 0H4V7a3 3 0 0 1 6 0H4m6 4h5m-5 4h5"/>',spark:'<path d="m13 2-9 12h7l-1 8 10-13h-7Z"/>',check:'<path d="m5 12 4 4L19 6"/>',circle:'<circle cx="12" cy="12" r="7"/>',heart:'<path d="M12 21S2 14 2 8a5 5 0 0 1 10-1A5 5 0 0 1 22 8c0 6-10 13-10 13Z"/>',sound:'<path d="m3 9 5 0 5-5v16l-5-5H3Zm13-2a7 7 0 0 1 0 10m3-13a11 11 0 0 1 0 16"/>',eye:'<path d="M2 12s4-7 10-7 10 7 10 7-4 7-10 7S2 12 2 12Z"/><circle cx="12" cy="12" r="3"/>',flag:'<path d="M5 22V3m0 0c6-5 8 5 15 0v10c-7 5-9-5-15 0"/>',download:'<path d="M12 3v12m-5-5 5 5 5-5M3 16v5h18v-5"/>',upload:'<path d="M12 16V4m-5 5 5-5 5 5M3 16v5h18v-5"/>',home:'<path d="m2 11 10-9 10 9M5 9v12h14V9M9 21v-8h6v8"/>',star:'<path d="m12 2 3 7 7 1-5 5 1 7-6-4-6 4 1-7-5-5 7-1Z"/>',fire:'<path d="M12 2c4 6-1 7 3 10 2-1 2-3 2-4 7 9 2 14-5 14S0 15 8 8c-1 4 1 5 2 5 3-3 1-6 2-11Z"/>'};
function icon(name){return '<svg class="icon" viewBox="0 0 24 24" aria-hidden="true">'+(paths[name]||paths.compass)+'</svg>';}
const btn=(action,label,attrs='',cls='')=>`<button data-action="${action}" ${attrs} class="${cls}">${label}</button>`;
const portrait=(k,cls='avatar')=>`<div class="portrait p${C.characters[k]?.index||0} ${cls}" role="img" aria-label="Portrait de ${esc(C.characters[k]?.name||'Maëlys')}"></div>`;
const speakerNames={mina:'Mina',ysse:'Ysse',custodian:'La Custode'};
const actorIndexes={maelys:0,ilyan:1,sera:2,orin:3,mina:0,ysse:1,custodian:2};
function actor(k,react=false,id=''){
 const known=Object.hasOwn(actorIndexes,k);if(!known)return '';
 const npc=['mina','ysse','custodian'].includes(k),side=k==='maelys'||((id.length+(react?1:0))%2)?'left':'right';
 return `<div class="actor actor-${npc?'npc':'hero'} actor-${k} blocking-${side} ${react?'is-reacting':''}" role="img" aria-label="${esc((C.characters[k]?.name||speakerNames[k])+(react?' réagit à votre choix':' vous écoute'))}"><i class="actor-frame actor-idle"></i><i class="actor-frame actor-react"></i>${C.characters[k]?portrait(k,'actor-fallback'):''}</div>`;
}
const Store={ok:true,get(k,fallback=null){try{const v=localStorage.getItem('nacre.v1.'+k);return v?JSON.parse(v):fallback;}catch(_){this.ok=false;return fallback;}},set(k,v){try{localStorage.setItem('nacre.v1.'+k,JSON.stringify(v));return true;}catch(_){this.ok=false;return false;}}};
const defaults={sfx:true,volume:.28,voice:false,voiceURI:'',font:1,motion:true,contrast:false,musicVolume:.25};
let settings={...defaults,...Store.get('settings',{})};
const audio=new window.NacreAudio();
let game=new Game(Store.get('meta',{achievements:[],endings:[],runs:0})),screen='title',modalType=null,toastTimer=0,lastSpeech='',lastSave=0,modalFocus=null;
function applySettings(){document.documentElement.style.setProperty('--fs',settings.font);document.body.classList.toggle('reduced',!settings.motion);document.body.classList.toggle('high-contrast',settings.contrast);Object.assign(audio,{sfx:settings.sfx,volume:settings.volume,voice:settings.voice,voiceURI:settings.voiceURI,musicVolume:settings.musicVolume});audio.setDuck(audio.duck);if(!settings.voice)audio.stopVoice();Store.set('settings',settings);}
function save(){if(!game.s)return;Store.set('auto',game.export());Store.set('meta',game.meta);lastSave=Date.now();}
function notify(message,error=false){const el=$('#toast');clearTimeout(toastTimer);el.textContent=message;el.classList.toggle('toast-error',error);el.classList.add('show');toastTimer=setTimeout(()=>el.classList.remove('show'),4200);}
function showNotices(){const n=game.drainNotices();if(n.length)notify(n.slice(-2).join(' · '));}
function action(fn,sound='click'){try{audio.init();fn();audio.effect(sound);save();render();showNotices();}catch(e){notify(e.message,true);}}
function logo(){return `<div class="logo">${icon('compass')}NACRE</div>`;}
function title(){const saved=Store.get('auto'),has=!!saved?.state;return `<div class="title-screen"><div class="title-top">${logo()}<span class="eyebrow">Une aventure à votre image</span></div><main id="main" class="title-main"><div class="ornament"><span class="eyebrow">Tout ce que la mer n’a pas oublié</span></div><h1>NACRE</h1><div class="title-sub">Les voix de l’abîme</div><p class="title-copy">Une ville se souvient de vous.<br>Vous ne vous souvenez plus d’elle.<br>À vous de choisir ce qui mérite de rester.</p><div class="title-actions">${has?btn('continue','Reprendre la traversée '+icon('arrow'),'','primary'):''}${btn('new','Commencer une traversée '+icon('arrow'),'',has?'':'primary')}${game.meta.endings.length?btn('ng','Nouveau Voyage+ '+icon('spark')):''}${game.meta.gallery.length?btn('gallery','Atlas des souvenirs '+icon('image')):''}${btn('saves','Sauvegardes '+icon('save'))}${btn('settings','Réglages '+icon('settings'))}</div></main><footer class="title-bottom"><span>Une aventure originale · Exploration, liens et conséquences</span><span>${btn('credits','Crédits','','quiet')} v${C.version}</span></footer></div>`;}
function header(){return `<header class="topbar">${logo()}<div class="top-right"><div class="chapter-mini"><span>Chapitre ${game.chapter+1}</span>${C.chapters[game.chapter]}</div><span class="pill">${icon('gem')} ${game.s.gold} éclats</span>${btn('help',icon('book'),'aria-label="Aide et commandes"','icon-btn quiet')}${btn('settings',icon('settings'),'aria-label="Réglages"','icon-btn quiet')}${btn('pause',icon('home'),'aria-label="Pause et menu"','icon-btn quiet')}</div></header>`;}
function meter(n,max,cls=''){return `<div class="meter ${cls}" role="meter" aria-valuemin="0" aria-valuemax="${max}" aria-valuenow="${n}" aria-label="${cls==='red'?'Vitalité adverse':'Vitalité'}"><i style="width:${Math.max(0,Math.min(100,n/max*100))}%"></i></div>`;}
function sidebar(){const s=game.s;const nav=[['map','map','Carte'],['journal','book','Journal'],['inventory','bag','Sac'],['companions','people','Liens'],['gallery','image','Atlas'],['saves','save','Sauvegarde']];return `<aside class="sidebar" aria-label="Personnage et navigation"><section class="stats-block"><div class="profile">${portrait('maelys')}<div><div class="name">Maëlys</div><small>Cartographe · Niveau ${s.level}</small></div></div><div class="stat"><div class="row between"><span>Vitalité</span><span>${s.hp} / ${game.maxHP}</span></div>${meter(s.hp,game.maxHP)}</div><div class="stat"><div class="row between"><span>Concentration</span><span>${s.focus} / ${game.maxFocus}</span></div><div class="focus-gems" aria-hidden="true">${Array.from({length:game.maxFocus},(_,i)=>`<i class="${i<s.focus?'on':''}"></i>`).join('')}</div></div><div class="status-row"><span>${icon('gem')} ${s.gold}</span><span>${icon('moon')} Résonance ${s.resonance}</span></div><div class="status-row"><span>Détermination ${s.resolve}</span><span title="Confiance des Libres / des Veilleurs">Libres ${s.rep.free} · Veilleurs ${s.rep.keepers}</span></div></section><section class="objective-card"><div class="eyebrow">${icon('flag')} Fil de la traversée</div><p>${game.objective()}</p></section><nav class="nav-grid" aria-label="Carnet de voyage">${nav.map(([a,i,t])=>btn(a,icon(i)+t)).join('')}</nav><section class="party-block"><div class="eyebrow" style="margin-bottom:13px;font-size:8px">À vos côtés</div><div class="party">${s.companions.length?s.companions.map(k=>btn('active',portrait(k,''),`data-id="${k}" aria-label="Choisir ${C.characters[k].name} comme partenaire" title="${C.characters[k].name} · ${C.characters[k].skill}" ${s.battle?'disabled':''}`,s.active===k?'selected':'')).join(''):'<small>La traversée commence.</small>'}</div><small style="font-size:9px">${s.active?C.characters[s.active].name+' · '+C.characters[s.active].skill:'Un rivage encore inconnu'}</small></section><div class="quick-help">E · Écho &nbsp; M · Carte &nbsp; J · Journal<br>Chaque rencontre peut changer une rive.</div><div class="sidebar-foot"><span>${icon('check')} ${Store.ok?'Progression sauvegardée':'Export manuel conseillé'}</span><span>${['Histoire','Aventure','Tactique'][['story','normal','tactical'].indexOf(s.difficulty)]}</span></div></aside>`;}
function particles(){return settings.motion?`<div class="particles" aria-hidden="true">${Array.from({length:16},(_,i)=>`<i class="mote" style="left:${(i*67+17)%100}%;top:${(i*43+23)%100}%;animation-delay:-${i*.71}s;animation-duration:${8+i%6}s"></i>`).join('')}</div>`:'';}
function stage(room,inside='',cls=''){return `<div class="stage camera-${room.camera||'default'} ${game.s.echo?'echo':''} ${cls}" role="group" aria-label="${esc(room.name)}"><div class="scene-art art art-${room.art}" role="img" aria-label="Décor : ${esc(room.name)}"></div><div class="scene-glass" aria-hidden="true"></div><div class="vignette"></div>${particles()}${inside}<div class="stage-caption">${game.s.echo?'L’écho · La mémoire sous le présent':esc(room.subtitle)}</div><div class="stage-compass" aria-hidden="true"><span>N</span></div></div>`;}
function heading(room){return `<div class="scene-heading"><div><div class="eyebrow">Chapitre ${String(game.chapter+1).padStart(2,'0')} · ${C.chapters[game.chapter]}</div><h1>${room.name}</h1></div><div class="scene-buttons">${btn('echo',icon('compass')+(game.s.echo?'Quitter l’écho':'Voir l’écho'),`aria-pressed="${game.s.echo}" ${game.busy?'disabled':''}`,'echo-toggle '+(game.s.echo?'active':''))}${btn('photo',icon('eye'),`aria-label="Contempler le décor"`,'icon-btn quiet')}</div></div>`;}
function exploration(){const s=game.s,r=C.rooms[s.room],spots=game.availableSpots();return heading(r)+stage(r,spots.map(p=>`<button class="hotspot ${game.isDone(p)?'done':''} ${p.echo?'echo-spot':''}" data-action="spot" data-id="${p.id}" style="left:${p.x}%;top:${p.y}%" aria-label="${esc(p.label)}${game.isDone(p)?' · déjà exploré':''}"><span class="spot-ring">${icon(game.isDone(p)?'check':p.echo?'moon':p.kind==='battle'?'sword':p.kind==='rest'?'fire':p.kind==='event'?'spark':p.kind==='companion'?'people':'gear')}</span><span class="spot-label">${esc(p.label)}</span></button>`).join(''))+`<div class="scene-bottom"><p class="scene-description">${r.description}</p><div class="exit-row">${r.exits.map(x=>btn('move',icon('arrow')+x.label,`data-id="${x.to}" ${game.has(x.requires)&&game.has(C.rooms[x.to].requires)?'':'disabled'} title="${esc(x.hint)}"`)).join('')}</div><details class="interaction-list" open><summary>Interactions du lieu · ${spots.length}</summary><div class="list-spots">${spots.map(p=>btn('spot',icon(game.isDone(p)?'check':'circle')+' '+p.label,`data-id="${p.id}"`,game.isDone(p)?'done':'')).join('')}</div></details></div>`;}
function dialogue(){
 const s=game.s,d=s.dialogue,e=C.events[d.id],r=C.rooms[s.room],paragraphs=C.paragraphs(d.id,s),reaction=d.reaction?e.choices[d.reaction.choice]:null;
 const speaker=reaction?.responder||e.speaker,who=C.characters[speaker],last=d.page===paragraphs.length-1,text=reaction?.response||paragraphs[d.page];
 return heading(r)+stage(r,actor(speaker,!!reaction,d.id),'talk-stage '+(reaction?'reaction-stage':''))+`<section class="narrative ${reaction?'choice-consequence':''}" aria-label="Dialogue"><div class="dialogue-head">${who?portrait(speaker):icon('compass')}<div><div class="speaker">${who?.name||speakerNames[speaker]||'Nacre'}</div><div class="dialogue-title">${reaction?'Conséquence immédiate':e.title}</div></div>${btn('read',icon('sound'),'aria-label="Lire ou arrêter la voix" style="margin-left:auto"','icon-btn quiet')}</div><div class="dialogue-text" id="dialogue-text" aria-live="polite">${esc(text)}</div>${last&&!reaction?`<div class="choice-list">${e.choices.map((c,i)=>{const locked=!game.has(c.requires)||(c.effects.bond&&s.bond&&s.bond!==c.effects.bond);return btn('choice',`<span class="choice-num">${String(i+1).padStart(2,'0')}</span><span class="text">${esc(d.id==='workshop_debt'&&s.flags.diver_saved?'« Vous n’avez plus à y aller seule. »':c.text)}${locked?`<small>${esc(c.effects.bond&&s.bond?'Une relation est déjà choisie ; l’amitié reste ouverte.':c.hint||'Condition non remplie.')}</small>`:''}</span>${icon(locked?'key':'arrow')}`,`data-index="${i}" ${locked?'disabled':''}`,'choice');}).join('')}</div>`:''}<div class="dialogue-footer"><div class="page-dots" aria-label="${reaction?'Réaction au choix':`Page ${d.page+1} sur ${paragraphs.length}`}">${reaction?'<i class="on consequence-dot"></i>':paragraphs.map((_,i)=>`<i class="${i<=d.page?'on':''}"></i>`).join('')}</div><span class="keyboard-tip">${reaction?'Ce choix est désormais inscrit dans votre carnet.':'Prenez le temps. Rien ne presse.'}</span>${reaction?btn('advance','Assumer ce choix '+icon('arrow'),'','primary'):last?'':btn('advance','Continuer '+icon('arrow'),'','primary')}</div></section>`;
}
function enemyArt(e,id){const idx=id==='bridge'?2:e.kind==='sentinel'?0:e.kind==='wraith'?1:3;return `<div class="enemy ${e.kind} enemy-art enemy-${idx}" role="img" aria-label="${esc(e.name)}"></div>`;}
function battle(){const s=game.s,b=s.battle,e=C.enemies[b.id],r=C.rooms[s.room],it=C.intents[game.intent()];const skills=[['strike','sword','Frapper',(12+(s.level-1)*2+(s.inventory.blade?3:0))+' dégâts · +1 concentration'],['guard','shield','Se protéger','Réduit les dégâts · +2 concentration'],['dodge','compass','Esquiver','Évite la frappe · 1 concentration'],['resonate','spark','Résonner','Traverse l’armure · 2 concentration'],['calm','moon','Apaiser','Une voie sans tuer · 1 concentration'],['companion','people',s.active?C.characters[s.active].skill:'Compagnon',b.cooldown?'Disponible dans '+b.cooldown+' tours':s.active?C.characters[s.active].skillText:'Recruter un compagnon']];return heading(r)+stage(r,`<div class="battle-top"><div class="enemy-title"><h3>${e.name}</h3><small>${b.hp} / ${e.hp} vitalité · Protection ${b.shield}</small>${meter(b.hp,e.hp,'red')}<div class="row between" style="margin-top:7px;font-size:9px"><span>Apaisement</span><span>${b.calm} / ${e.calm}</span></div>${meter(b.calm,e.calm,'gold')}</div>${b.result?'':`<div class="intent"><strong>${icon(it.icon)} ${it.name}</strong>${it.desc}</div>`}</div>${enemyArt(e,b.id)}`)+`<section class="narrative"><div class="row between"><span class="eyebrow">Confrontation · Tour ${b.turn+1}</span><small>Vitalité ${s.hp}/${game.maxHP} · Concentration ${s.focus}/${game.maxFocus}</small></div><div class="battle-log" role="log" aria-live="polite">${b.log.slice(-4).map(x=>`<p>${esc(x)}</p>`).join('')}</div>${b.result?`<div class="result"><h3>${b.result==='win'?'Le passage est libre':'La traversée ne s’arrête pas ici'}</h3><p>${b.result==='win'?'Votre décision laisse une trace dans la ville.':'Vos compagnons vous ramènent au repos. Préparez une nouvelle tentative.'}</p>${btn('end-battle',b.result==='win'?'Poursuivre la traversée':'Rejoindre le refuge','','primary')}</div>`:`<div class="combat-actions">${skills.map(([a,i,t,sub])=>{const disabled=(['dodge','calm'].includes(a)&&s.focus<1)||(a==='resonate'&&s.focus<2)||(a==='companion'&&(!s.active||b.cooldown));return btn('combat',icon(i)+`<span><strong>${t}</strong><small>${sub}</small></span>`,`data-id="${a}" ${disabled?'disabled':''}`);}).join('')}</div><div class="battle-items">${['tonic','tea','bomb'].map(k=>btn('combat-item',icon(C.items[k].icon)+' '+C.items[k].name+' ×'+(s.inventory[k]||0),`data-id="${k}" ${s.inventory[k]?'':'disabled'}`)).join('')}${btn('flee','Se replier','','quiet')}</div>`}</section>`;}
function puzzle(){const q=game.s.puzzle,p=C.puzzles[q.id],r=C.rooms[game.s.room];return heading(r)+stage(r,'','puzzle-stage')+`<section class="puzzle-console"><div class="eyebrow">Mécanisme de Nacre</div><h2 style="margin:7px 0 14px">${p.name}</h2><p class="muted">${p.text}</p><div class="puzzle-dials">${q.values.map((n,i)=>`<div class="dial ${p.type==='lights'&&n?'lit':''}"><small>${p.labels[i]}</small>${btn('dial',p.type==='lights'?(n?'◉':'○'):p.options[n],`data-index="${i}" aria-label="${p.labels[i]} : ${p.type==='lights'?(n?'allumé':'éteint'):p.options[n]}. Tourner"`)}</div>`).join('')}</div>${q.hint?`<p class="puzzle-hint">${p.hint}</p>`:''}<div class="puzzle-actions">${btn('check-puzzle','Accorder le mécanisme','','primary')}${btn('hint','Un indice '+icon('eye'))}${btn('reset-puzzle','Remettre à zéro')}${btn('close-puzzle','Revenir plus tard','','quiet')}</div></section>`;}
function ending(){const s=game.s,e=C.endings[s.ending];return `<div class="ending-screen"><div class="ending-background art art-${e.art}"></div><main id="main" class="ending-content">${logo()}<div class="eyebrow" style="margin-top:60px">Épilogue · ${e.tag}</div><h1>${e.title}</h1>${e.lines.map(p=>`<p>${p}</p>`).join('')}<hr><h3>Ce que votre traversée a changé</h3>${game.epilogue().map(p=>`<p>${p}</p>`).join('')}<div class="ending-stats"><div><strong>${game.echoCount}/6</strong><small>Souvenirs retrouvés</small></div><div><strong>${C.quests.filter(q=>q.steps.every(([f])=>s.flags[f])).length}/8</strong><small>Histoires accomplies</small></div><div><strong>${game.meta.endings.length}/6</strong><small>Rivages découverts</small></div></div><div class="modal-buttons">${btn('ng','Un nouveau voyage +','','primary')}${btn('export','Exporter cette traversée '+icon('download'))}${btn('gallery','Ouvrir l’atlas')}${btn('title','Retour au menu','','quiet')}</div><p class="credit" style="font-family:inherit;font-size:12px;margin-top:45px">Merci d’avoir traversé Nacre. Chaque rive est une fin complète ; aucune ne remplace les autres.</p></main></div>`;}
function render(){const focus=document.activeElement?.dataset;const restore=focus?.action?{...focus}:null;if(screen==='title')$('#app').innerHTML=title();else if(game.s?.ending)$('#app').innerHTML=ending();else if(game.s)$('#app').innerHTML=`<div class="shell">${header()}<div class="game-grid"><main class="play-column" id="main">${game.s.dialogue?dialogue():game.s.battle?battle():game.s.puzzle?puzzle():exploration()}</main>${sidebar()}</div></div>`;else{screen='title';$('#app').innerHTML=title();}if(restore){const candidates=[...document.querySelectorAll('#app [data-action]')];const match=candidates.find(x=>Object.keys(restore).every(k=>x.dataset[k]===restore[k])&&!x.disabled);if(match)match.focus({preventScroll:true});}
 const d=game.s?.dialogue,key=screen==='game'&&d?d.id+':'+d.page+':'+(d.reaction?.choice??''):'';if(key!==lastSpeech){lastSpeech=key;audio.stopVoice();if(key&&settings.voice){const e=C.events[d.id];audio.speak(d.reaction?e.choices[d.reaction.choice].response:C.paragraphs(d.id,game.s)[d.page]);}}
}
function openModal(type,title,body){modalType=type;modalFocus=document.activeElement;$('#modal-content').innerHTML=`<div class="modal-head"><h2 id="modal-title">${title}</h2>${btn('close',icon('close'),'aria-label="Fermer"','icon-btn quiet')}</div><div class="modal-body">${body}</div>`;if(!$('#modal').open)$('#modal').showModal();audio.stopVoice();}
function closeModal(){if($('#modal').open)$('#modal').close();modalType=null;if(modalFocus?.isConnected)modalFocus.focus({preventScroll:true});}
function needGame(){if(!game.s||screen==='title'){notify('Commencez ou reprenez une traversée.');return false;}return true;}
function newDialog(ng=false){const exists=Store.get('auto');openModal('new',ng?'Un nouveau voyage +':'Choisir votre traversée',`${ng?'<p>Votre atlas, vos fins et vos accomplissements vous accompagnent. L’histoire et les liens recommencent, avec quelques matériaux supplémentaires.</p>':'<p>Une histoire complète, à votre rythme. La difficulté change les dégâts reçus ; les dialogues et les fins restent les mêmes.</p>'}${exists?'<div class="tutorial-box"><small>La traversée actuelle sera gardée dans « Traversée précédente ». Vos emplacements manuels et votre atlas restent disponibles.</small></div>':''}<div class="difficulty-cards">${[['story','Histoire','Pour explorer et suivre les liens. Dégâts reçus réduits de 45 %.'],['normal','Aventure','L’équilibre conseillé. Préparez vos ressources et lisez les intentions.'],['tactical','Tactique','Dégâts reçus augmentés de 25 %. Chaque tour compte.']].map(([k,t,d])=>btn('start',`<strong>${t}</strong><small>${d}</small>`,`data-id="${k}" data-ng="${ng?'1':'0'}"`)).join('')}</div>`);}
function mapModal(){if(!needGame())return;const s=game.s,lines=[];for(const[id,r]of Object.entries(C.rooms))for(const x of r.exits)if(id<x.to){const t=C.rooms[x.to];lines.push(`<line x1="${r.map[0]}" y1="${r.map[1]}" x2="${t.map[0]}" y2="${t.map[1]}"/>`);}openModal('map','L’atlas des rivages',`<p class="muted">${game.busy?'Terminez l’action en cours pour voyager.':'Choisissez un lieu accessible. Le voyage ne consomme aucune ressource.'}</p><div class="map-canvas"><svg class="map-lines" viewBox="0 0 100 100" preserveAspectRatio="none" aria-hidden="true">${lines.join('')}</svg>${Object.entries(C.rooms).map(([id,r])=>btn('travel',`<i></i><span>${game.has(r.requires)?r.name:'Inexploré'}</span>`,`data-id="${id}" style="left:${r.map[0]}%;top:${r.map[1]}%" ${!game.has(r.requires)||game.busy?'disabled':''}`,'map-node '+(id===s.room?'current':''))).join('')}<div class="map-legend">La mer garde encore des chemins.</div></div><div class="map-list">${Object.entries(C.rooms).filter(([,r])=>game.has(r.requires)).map(([id,r])=>btn('travel',icon(id===s.room?'compass':'arrow')+' '+r.name,`data-id="${id}" ${game.busy?'disabled':''}`)).join('')}</div>`);}
function journalModal(history=false){if(!needGame())return;const s=game.s;openModal('journal','Le carnet de Maëlys',`<div class="tabs">${btn('journal','Quêtes','',history?'':'active')}${btn('history','Souvenirs de parcours','',history?'active':'')}</div>${history?s.journal.length?s.journal.slice().reverse().map(j=>`<div class="quest"><div class="eyebrow">${esc(C.rooms[j.room]?.name||'La traversée')}</div><p style="margin-top:9px">${esc(j.text)}</p></div>`).join(''):'<p>Votre première page reste à écrire.</p>':C.quests.map(q=>{const started=q.id==='main'||q.steps.some(([f])=>s.flags[f]);const done=q.steps.every(([f])=>s.flags[f]);const first=q.steps.findIndex(([f])=>!s.flags[f]);return `<article class="quest"><div class="row between"><h3>${started?q.title:'Une histoire à rencontrer'}</h3>${done?icon('check'):icon('book')}</div><p>${started?q.desc:'Explorez Nacre et prenez le temps d’écouter ses habitants.'}</p>${started?`<div class="quest-steps">${q.steps.map(([f,t],i)=>`<div class="quest-step ${s.flags[f]?'done':i===first?'next':''}">${icon(s.flags[f]?'check':'circle')}<span>${t}</span></div>`).join('')}</div>`:''}</article>`;}).join('')}`);}
function inventoryModal(mode='inventory'){if(!needGame())return;const s=game.s,craft=mode==='craft',shop=mode==='shop';const list=craft?C.recipes.map(r=>r.id):shop?['herb','scrap','glass','tonic','tea']:Object.keys(s.inventory).filter(k=>s.inventory[k]>0);const prices={herb:3,glass:4,scrap:3,tonic:7,tea:6};openModal(mode,craft?'L’établi de Séra':shop?'L’étal des provisions':'Votre sac de voyage',`<div class="row between" style="margin-bottom:20px"><small>${craft?'Les améliorations sont équipées automatiquement.':shop?'Chaque échange est local au quartier.':'Équipements actifs automatiquement · Objets de quête conservés'}</small><span class="pill">${s.gold} éclats</span></div><div class="inventory-grid">${list.map(k=>{const it=C.items[k],r=C.recipes.find(x=>x.id===k);return `<article class="item-card"><div class="row between">${icon(it.icon)}<span class="quantity">× ${s.inventory[k]||0}</span></div><h4 style="margin-top:12px">${it.name}</h4><p>${it.desc}</p>${craft?`<p>${Object.entries(r.cost).map(([id,n])=>`${n} ${C.items[id].name} (${s.inventory[id]||0})`).join(' + ')}</p>${btn('craft-item',r.unique&&s.inventory[k]?'Déjà équipé':'Fabriquer',`data-id="${k}" ${game.canCraft(k)&&!game.busy?'':'disabled'}`)}`:shop?btn('buy',`Échanger · ${prices[k]} éclats`,`data-id="${k}" ${s.gold>=prices[k]&&!game.busy?'':'disabled'}`):['tonic','tea'].includes(k)?btn('use','Utiliser',`data-id="${k}" ${game.busy?'disabled':''}`):it.kind==='equip'?'<p class="gold">Équipé</p>':''}</article>`;}).join('')}</div>`);}
function companionsModal(){if(!needGame())return;const s=game.s;openModal('companions','Les liens de la traversée',`<p class="muted">Le partenaire actif vous aide en combat. Les conversations intimes se déroulent au camp des remparts.</p>${s.companions.map(k=>{const c=C.characters[k],v=s.relations[k],label=s.flags['bond_'+k]?(s.bond===k?'Un amour choisi':'Une amitié profonde'):v>=4?'La confiance s’installe':v>=2?'Une présence familière':'Apprendre à se connaître';return `<article class="companion-card">${portrait(k,'')}<div><div class="eyebrow" style="font-size:8px">${c.role}</div><h3>${c.name}</h3><p><em>« ${c.quote} »</em></p><div class="affinity" aria-hidden="true">${Array.from({length:6},(_,i)=>`<i class="${i<Math.max(1,Math.min(6,Math.ceil(v/2)))?'on':''}"></i>`).join('')}</div><small>${label}</small><p>${c.skill} · ${c.skillText}</p>${btn('active',s.active===k?'Partenaire actif':'Choisir comme partenaire',`data-id="${k}" ${s.battle?'disabled':''}`)} ${s.room==='camp'?btn('talk-menu','Parler',`data-id="${k}" ${game.busy?'disabled':''}`):btn('travel','Retrouver au camp',`data-id="camp" ${game.busy?'disabled':''}`)}</div></article>`;}).join('')||'<p>Le voyage vous réservera des rencontres.</p>'}`);}
function talkModal(k){const opts=game.companionOptions(k);openModal('talk',C.characters[k].name,`<div class="row">${portrait(k)}<p class="serif">${opts.length?'Le feu laisse de la place aux mots.':'Vous partagez un silence tranquille. Certaines conversations demandent de la confiance, ou quelque chose à rapporter.'}</p></div><div class="choice-list" style="margin-top:20px">${opts.map(o=>btn('talk',o.label,`data-id="${k}" data-event="${o.id}"`,'choice')).join('')}</div>`);}
function galleryModal(tab='art'){const visited=game.s?.visited||[],arts=[...new Set(Object.values(C.rooms).map(r=>r.art))];openModal('gallery','L’atlas des souvenirs',`<div class="tabs">${btn('gallery','Rivages','',tab==='art'?'active':'')}${btn('endings','Fins · '+game.meta.endings.length+'/6','',tab==='endings'?'active':'')}${btn('achievements','Accomplissements','',tab==='achievements'?'active':'')}</div>${tab==='art'?`<div class="art-grid">${arts.map(a=>{const r=Object.entries(C.rooms).find(([,r])=>r.art===a),unlocked=game.meta.gallery.includes(a)||visited.some(id=>C.rooms[id].art===a);return btn('art',`<span>${unlocked?r[1].name:'Rivage inconnu'}</span>`,`data-id="${a}" ${unlocked?'':'disabled'}`,'gallery-art art '+(unlocked?'art-'+a:'locked'));}).join('')}</div>`:tab==='endings'?`<div class="quest-steps">${Object.entries(C.endings).map(([id,e])=>`<div class="quest"><h3>${game.meta.endings.includes(id)?e.title:'Une autre rive'}</h3><p>${game.meta.endings.includes(id)?e.lines[0]:'Cette issue se découvrira dans une autre traversée.'}</p></div>`).join('')}</div>`:`<div class="achievement-grid">${C.achievements.map(a=>`<div class="achievement ${game.meta.achievements.includes(a.id)?'unlocked':''}"><strong>${icon(game.meta.achievements.includes(a.id)?'star':'circle')} ${a.name}</strong><p>${a.desc}</p></div>`).join('')}</div>`}`);}
function savesModal(){const autos=Store.get('auto'),previous=Store.get('previous');const rows=[['auto','Sauvegarde automatique',autos],...[1,2,3].map(n=>['slot'+n,'Emplacement '+n,Store.get('slot'+n)]),['previous','Traversée précédente',previous]];openModal('saves','Les traces de votre passage',`<p class="muted">Trois emplacements manuels, une sauvegarde automatique et votre traversée précédente. L’export permet de changer d’appareil.</p>${rows.map(([k,label,p])=>`<div class="save-card"><div class="row between wrap"><div><strong>${label}</strong><small>${p?.state?`${esc(C.rooms[p.state.room]?.name||'Partie')} · Niveau ${p.state.level} · ${new Date(p.exportedAt).toLocaleString('fr-FR')}`:'Aucune trace ici'}</small></div><div class="row">${k.startsWith('slot')?btn('manual-save','Sauvegarder',`data-id="${k}" ${game.s&&screen==='game'?'':'disabled'}`):''}${btn('load-save','Charger',`data-id="${k}" ${p?'':'disabled'}`)}</div></div></div>`).join('')}<div class="modal-buttons">${btn('export',icon('download')+' Exporter JSON',`${game.s?'':'disabled'}`,'primary')}${btn('import',icon('upload')+' Importer JSON')}</div><p class="credit" style="margin-top:18px">Les sauvegardes restent dans ce navigateur. Déplacer le fichier ou changer de navigateur peut changer leur emplacement : gardez un export.</p>`);}
function settingsModal(){const voices=audio.voices();openModal('settings','Prendre le temps',`<div class="setting"><div><label for="sfx">Effets sonores</label><small>Touches, mécanismes et confrontations.</small></div><input id="sfx" data-setting="sfx" type="checkbox" ${settings.sfx?'checked':''}></div><div class="setting"><label for="volume">Volume des effets</label><input id="volume" data-setting="volume" type="range" min="0" max="0.8" step="0.05" value="${settings.volume}"></div><div class="setting"><div><label for="voice">Lecture vocale</label><small>Voix françaises de votre appareil. Les autres sons baissent à 25 % pendant la lecture.</small></div><input id="voice" data-setting="voice" type="checkbox" ${settings.voice?'checked':''} ${'speechSynthesis'in window?'':'disabled'}></div><div class="setting"><label for="voiceURI">Voix</label><select id="voiceURI" data-setting="voiceURI"><option value="">Automatique · appareil</option>${voices.map(v=>`<option value="${esc(v.voiceURI)}" ${settings.voiceURI===v.voiceURI?'selected':''}>${esc(v.name)}</option>`).join('')}</select></div><div class="row wrap" style="margin-top:13px">${btn('preview-voice','Écouter un aperçu')}${btn('stop-voice','Arrêter')}</div><div class="setting"><label for="font">Taille du texte</label><select id="font" data-setting="font">${[[1,'Normale'],[1.15,'Grande'],[1.3,'Très grande']].map(([v,t])=>`<option value="${v}" ${+settings.font===v?'selected':''}>${t}</option>`).join('')}</select></div><div class="setting"><div><label for="motion">Mouvements et particules</label><small>Respecte aussi la préférence de mouvement réduit de l’appareil.</small></div><input id="motion" data-setting="motion" type="checkbox" ${settings.motion?'checked':''}></div><div class="setting"><label for="contrast">Contraste renforcé</label><input id="contrast" data-setting="contrast" type="checkbox" ${settings.contrast?'checked':''}></div>${game.s?`<div class="setting"><div><label for="difficulty">Difficulté des confrontations</label><small>Modifiable en dehors d’une confrontation.</small></div><select id="difficulty" ${game.s.battle?'disabled':''}>${[['story','Histoire'],['normal','Aventure'],['tactical','Tactique']].map(([v,t])=>`<option value="${v}" ${game.s.difficulty===v?'selected':''}>${t}</option>`).join('')}</select></div>`:''}<h3 style="margin-top:25px">Vos musiques</h3><p class="credit">Aucune musique n’est fournie. Ajoutez vos MP3, WAV ou autres fichiers audio : lecture en boucle, atténuée pendant la voix. Les fichiers restent sur votre appareil et devront être sélectionnés à chaque session.</p><div class="row wrap">${btn('music',icon('sound')+' Ajouter des musiques')}${audio.tracks.length?btn('music-toggle',audio.player.paused?'Reprendre':'Pause')+btn('music-clear','Retirer'):''}</div>${audio.tracks.length?`<p class="credit" style="margin-top:12px">${audio.tracks.length} pistes · ${esc(audio.tracks[audio.trackIndex].name)}</p>`:''}<div class="setting"><label for="musicVolume">Volume musical</label><input id="musicVolume" data-setting="musicVolume" type="range" min="0" max="1" step="0.05" value="${settings.musicVolume}"></div>`);}
function helpModal(){openModal('help','Votre première traversée',C.help.map(([t,d])=>`<section class="quest"><h3>${t}</h3><p>${d}</p></section>`).join(''));}
function exportSave(){if(!game.s)return;const data=JSON.stringify(game.export(),null,2);download(data,'nacre-sauvegarde-'+new Date().toISOString().slice(0,10)+'.json','application/json');notify('Sauvegarde exportée.');}
function download(data,name,type){const url=URL.createObjectURL(new Blob([data],{type})),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),10000);}
function confirmModal(title,text,actionName,attrs=''){openModal('confirm',title,`<p>${text}</p><div class="modal-buttons">${btn(actionName,'Confirmer',attrs,'primary')}${btn('close','Annuler')}</div>`);}
const handlers={
 new:()=>newDialog(false),ng:()=>newDialog(true),
 start:b=>{const prev=Store.get('auto');if(prev)Store.set('previous',prev);closeModal();action(()=>{game.newGame(b.dataset.id,b.dataset.ng==='1');screen='game';});if(!Store.get('tutorial')){Store.set('tutorial',true);openModal('welcome','Bienvenue à Nacre',`<div class="tutorial-box"><p>Explorez les repères dans les décors. La boussole révèle une seconde couche de souvenirs. La carte vous permet de revenir librement dans les lieux ouverts.</p><p>Les confrontations attendent votre choix : lisez l’intention adverse. Au camp, le repos est gratuit.</p></div><p>Votre progression est enregistrée après chaque action. Pour la conserver ailleurs, exportez-la depuis « Sauvegardes ».</p>${btn('close','Commencer ma traversée','','primary')}`);}},
 continue:()=>{action(()=>{game.load(Store.get('auto'));screen='game';});},
 close:closeModal,echo:()=>action(()=>game.toggleEcho(),'echo'),
 advance:()=>action(()=>game.advance()),choice:b=>action(()=>game.choose(+b.dataset.index)),
 move:b=>action(()=>game.move(b.dataset.id),'step'),
 travel:b=>{if(game.busy){notify('Terminez l’action en cours.');return;}closeModal();action(()=>game.move(b.dataset.id),'step');},
 spot:b=>{let kind,target;action(()=>{target=game.availableSpots().find(p=>p.id===b.dataset.id)?.target;kind=game.interact(b.dataset.id);});if(kind==='craft'||kind==='shop')inventoryModal(kind);if(kind==='companion')talkModal(target);},
 map:mapModal,journal:()=>journalModal(),history:()=>journalModal(true),inventory:()=>inventoryModal(),companions:companionsModal,
 active:b=>{action(()=>game.setCompanion(b.dataset.id));if(modalType==='companions')companionsModal();},
 'talk-menu':b=>talkModal(b.dataset.id),talk:b=>{closeModal();action(()=>game.talk(b.dataset.id,b.dataset.event));},
 'craft-item':b=>{action(()=>game.craft(b.dataset.id),'success');inventoryModal('craft');},buy:b=>{action(()=>game.buy(b.dataset.id));inventoryModal('shop');},use:b=>{action(()=>game.useItem(b.dataset.id));inventoryModal();},
 combat:b=>action(()=>game.combat(b.dataset.id),'hit'),'combat-item':b=>action(()=>game.combat('item',b.dataset.id)),
 flee:()=>confirmModal('Se replier ?','Vous retrouverez vos forces au refuge. Les objets consommés pendant le combat ne seront pas remboursés.','do-flee'),
 'do-flee':()=>{closeModal();action(()=>game.retreat());},'end-battle':()=>action(()=>game.endBattle(),'success'),
 dial:b=>action(()=>game.puzzleStep(+b.dataset.index)),hint:()=>action(()=>{game.s.puzzle.hint=true;}),'reset-puzzle':()=>action(()=>game.resetPuzzle()),'check-puzzle':()=>action(()=>game.checkPuzzle(),'success'),'close-puzzle':()=>action(()=>game.closePuzzle()),
 gallery:()=>galleryModal(),endings:()=>galleryModal('endings'),achievements:()=>galleryModal('achievements'),
 art:b=>openModal('art','Un rivage dans l’atlas',`<div class="art-large art art-${b.dataset.id}" role="img" aria-label="Décor de Nacre"></div>${btn('gallery','Retour à l’atlas','style="margin-top:16px"')}`),
 photo:()=>{const r=C.rooms[game.s.room];openModal('photo',r.name,`<div class="art-large art art-${r.art}" role="img" aria-label="${r.name}"></div>`);},
 saves:savesModal,'manual-save':b=>{if(Store.get(b.dataset.id))confirmModal('Remplacer cet emplacement ?','Seul cet emplacement manuel sera remplacé.','do-save',`data-id="${b.dataset.id}"`);else handlers['do-save'](b);},
 'do-save':b=>{const ok=Store.set(b.dataset.id,game.export());notify(ok?'Emplacement sauvegardé.':'Cet emplacement n’a pas pu être enregistré. Exportez la partie.',!ok);savesModal();},
 'load-save':b=>confirmModal('Reprendre cette trace ?','La progression actuelle sera gardée dans « Traversée précédente ».','do-load',`data-id="${b.dataset.id}"`),
 'do-load':b=>{const data=Store.get(b.dataset.id);try{Game.validate(data);const old=Store.get('auto');if(old)Store.set('previous',old);closeModal();action(()=>{game.load(data);screen='game';});}catch(e){notify(e.message,true);}},
 export:exportSave,import:()=>$('#import-file').click(),
 settings:settingsModal,help:helpModal,
 read:()=>{if(audio.duck){audio.stopVoice();return;}audio.voice=true;const d=game.s.dialogue;if(d){const e=C.events[d.id];audio.speak(d.reaction?e.choices[d.reaction.choice].response:C.paragraphs(d.id,game.s)[d.page]);}},
 'preview-voice':()=>{audio.voice=true;audio.speak('La mer garde les noms que nous avons oubliés. Bienvenue à Nacre.');},'stop-voice':()=>audio.stopVoice(),
 music:()=>$('#music-files').click(),'music-toggle':()=>{if(audio.player.paused)audio.player.play().catch(()=>notify('Sélectionnez à nouveau vos fichiers audio.'));else audio.player.pause();settingsModal();},'music-clear':()=>{audio.clearTracks();settingsModal();},
 pause:()=>openModal('pause','Une halte dans la traversée',`<p>Votre progression est sauvegardée. Reprenez quand vous le souhaitez.</p><div class="modal-buttons">${btn('close','Continuer','','primary')}${btn('saves','Sauvegardes')}${btn('settings','Réglages')}${btn('title','Retour au menu')}</div>`),
 title:()=>{save();audio.pause();closeModal();screen='title';render();},
 credits:()=>openModal('credits','NACRE · Les voix de l’abîme',`<p>Une aventure originale conçue pour Eymrick. Scénario, personnages, mécaniques et interface créés pour ce projet.</p><p>Illustrations générées avec l’outil de création d’images OpenAI. Effets sonores synthétisés localement. Aucun fichier musical fourni, aucun compte requis, aucune télémétrie.</p><p>Version ${C.version} · Jeu indépendant complet. Le projet inclut les sources modifiables, les tests et la documentation.</p><p class="credit">Nacre traite de mémoire, de deuil et de responsabilité. Tous les personnages de romance sont adultes.</p>`)
};
document.addEventListener('click',e=>{const b=e.target.closest('[data-action]');if(!b||b.disabled)return;const h=handlers[b.dataset.action];if(h)h(b);});
document.addEventListener('change',e=>{const el=e.target;if(el.dataset.setting){const k=el.dataset.setting;settings[k]=el.type==='checkbox'?el.checked:['font','volume','musicVolume'].includes(k)?Number(el.value):el.value;applySettings();render();}if(el.id==='difficulty'&&game.s&&!game.s.battle){game.s.difficulty=el.value;save();render();}});
$('#modal').addEventListener('close',()=>{audio.stopVoice();modalType=null;});
$('#modal').addEventListener('click',e=>{if(e.target===$('#modal')){const r=$('#modal').getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)closeModal();}});
$('#import-file').addEventListener('change',async e=>{const f=e.target.files[0];e.target.value='';if(!f)return;try{if(f.size>1500000)throw Error('Ce fichier est trop volumineux pour une sauvegarde.');const p=JSON.parse(await f.text());Game.validate(p);const old=Store.get('auto');if(old)Store.set('previous',old);closeModal();action(()=>{game.load(p);screen='game';});notify('Traversée importée.');}catch(err){notify(err.message||'Sauvegarde illisible.',true);}});
$('#music-files').addEventListener('change',e=>{const n=audio.loadTracks(e.target.files);e.target.value='';settingsModal();notify(n+' piste(s) ajoutée(s) à cette session.');});
document.addEventListener('keydown',e=>{if(e.key==='Escape'){if($('#modal').open){e.preventDefault();closeModal();}else if(screen==='game')handlers.pause();return;}if($('#modal').open||['INPUT','SELECT','TEXTAREA'].includes(e.target.tagName)||e.ctrlKey||e.metaKey||e.altKey)return;if(screen!=='game')return;const key=e.key.toLowerCase(),f={m:handlers.map,j:handlers.journal,i:handlers.inventory,c:handlers.companions};if(f[key]){e.preventDefault();f[key]();}else if(key==='e'&&!game.busy){e.preventDefault();handlers.echo();}});
setInterval(()=>{if(screen==='game'&&game.s&&!document.hidden&&!$('#modal').open){game.s.playSeconds+=15;if(Date.now()-lastSave>30000)save();}},15000);
window.addEventListener('pagehide',save);document.addEventListener('visibilitychange',()=>{if(document.hidden){save();audio.stopVoice();}});
if('speechSynthesis'in window)speechSynthesis.addEventListener('voiceschanged',()=>{if(modalType==='settings')settingsModal();});
applySettings();render();
})();
